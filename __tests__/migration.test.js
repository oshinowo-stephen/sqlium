const test = require('ava')
const {
  runMigration,
  runMigrations,
  generateMigration,
} = require('../lib/managers/migration')

test('generate migration', async (t) => {
  t.pass('unimplemented!')
})

test('run migrations', async (t) => {
  t.pass('unimplemented!')
})

test('rollback migrations', async (t) => {
  t.pass('unimplemented!')
})
